# Quarto rendered output (the site is built by CI, never committed)
/_site/
/.quarto/

# Private raw sources (e.g. handwritten scans) — kept locally, never committed
/_source/

# Jupyter / Python
.ipynb_checkpoints/
__pycache__/
.venv/
venv/
*.pyc

# OS / editor cruft
.DS_Store
.vscode/

**/*.quarto_ipynb
